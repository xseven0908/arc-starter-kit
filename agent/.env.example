# Wallet the agent signs with (needs native USDC for gas + spending)
PRIVATE_KEY=

ARC_RPC_URL=https://rpc.testnet.arc.io
PAYMENT_LOG_ADDRESS=0xB273B3D1f6fD43cc7EfaC625bfF56f114895adB5

# Guardrails: max USDC per send_payment call, and comma-separated allowed recipients.
# send_payment rejects everything if ALLOWLIST is empty.
SPEND_CAP_USDC=1.0
ALLOWLIST=
