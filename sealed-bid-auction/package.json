{
  "name": "sealed-bid-auction",
  "version": "0.1.0",
  "private": true,
  "description": "Commit-reveal sealed-bid auction settled in native USDC on Arc Testnet",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network arcTestnet",
    "demo": "hardhat run scripts/demo.ts --network arcTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
